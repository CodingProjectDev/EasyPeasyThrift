'use client';
import { FormEvent, useMemo, useState } from 'react';
import Link from 'next/link';
import { Check, QrCode, Truck } from 'lucide-react';
import { useStore } from '@/components/store-provider';
import { money } from '@/lib/format';
import { Order, PaymentMethod } from '@/lib/types';

export default function CheckoutPage(){
  const { cartProducts, placeLocalOrder, promos, settings }=useStore();
  const [method,setMethod]=useState<PaymentMethod>('COD'); const [proof,setProof]=useState<File|null>(null); const [proofPreview,setProofPreview]=useState(''); const [txid,setTxid]=useState(''); const [promo,setPromo]=useState(''); const [placed,setPlaced]=useState<Order|null>(null); const [busy,setBusy]=useState(false);
  const subtotal=cartProducts.reduce((s,x)=>s+x.product.price*x.quantity,0); const shipping=subtotal>=settings.freeShippingThreshold||subtotal===0?0:settings.shippingFee;
  const validPromo=useMemo(()=>promos.find(p=>p.active&&p.code.toLowerCase()===promo.trim().toLowerCase()&&new Date(p.expiresAt)>=new Date()),[promo,promos]); const discount=validPromo?(validPromo.type==='percentage'?subtotal*validPromo.value/100:Math.min(subtotal,validPromo.value)):0; const total=Math.max(0,subtotal+shipping-discount);
  const handleProof=(file?:File)=>{if(!file)return;setProof(file);const r=new FileReader();r.onload=()=>setProofPreview(String(r.result));r.readAsDataURL(file)};
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); if(!cartProducts.length)return; if(method==='QR'&&(!proof||!txid.trim())){alert('For QR Payment, upload payment proof and enter the transaction/reference ID.');return} setBusy(true); const fd=new FormData(e.currentTarget); let proofUrl='';
    if(method==='QR'&&proof){try{const up=new FormData();up.append('file',proof);const r=await fetch('/api/payment-proof',{method:'POST',body:up});if(r.ok){const j=await r.json();proofUrl=j.path||''}}catch{}}
    const order:Order={id:`EP-${Date.now().toString().slice(-8)}`,createdAt:new Date().toISOString(),customer:{name:String(fd.get('name')),email:String(fd.get('email')),phone:String(fd.get('phone')),address:String(fd.get('address')),city:String(fd.get('city')),postalCode:String(fd.get('postalCode'))},items:cartProducts.map(({product,quantity})=>({productId:product.id,name:product.name,price:product.price,quantity})),subtotal,shipping,discount,total,paymentMethod:method,paymentProofName:proof?.name,paymentProofDataUrl:proofPreview,transactionId:method==='QR'?txid:undefined,status:method==='QR'?'Payment Verification Required':'Pending'};
    try{const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...order,paymentProofPath:proofUrl,promoCode:validPromo?.code||null})});if(r.ok){const j=await r.json();order.id=j.orderId||order.id}}catch{} placeLocalOrder(order);setPlaced(order);setBusy(false);window.scrollTo({top:0,behavior:'smooth'});
  }
  if(placed)return <div className="container content-page"><div className="success-box"><div className="success-icon"><Check size={34}/></div><span className="eyebrow">Order {placed.id}</span><h2 style={{marginTop:12}}>Order placed.</h2><p>{placed.paymentMethod==='QR'?'Your payment proof was submitted. The order is now Payment Verification Required until the admin approves it.':'Your COD order is Pending and will move to Processing after review.'}</p><div className="hero-actions" style={{justifyContent:'center'}}><Link className="btn" href="/account/orders">View order</Link><Link className="btn secondary" href="/shop">Keep shopping</Link></div></div></div>;
  if(!cartProducts.length)return <div className="container content-page"><div className="empty-state"><h2>Your cart is empty.</h2><Link className="btn" href="/shop">Back to shop</Link></div></div>;
  return <div className="container"><div className="page-hero"><span className="eyebrow">Almost yours</span><h1>Checkout.</h1><p>No card forms here. Choose Cash on Delivery or pay by QR and upload your proof.</p></div><form onSubmit={submit} className="checkout-layout"><div className="stack"><section className="panel"><h3>Delivery details</h3><div className="checkout-form"><div className="field full"><label>Full name</label><input className="control" name="name" required/></div><div className="field"><label>Email</label><input className="control" name="email" type="email" required/></div><div className="field"><label>Phone</label><input className="control" name="phone" required/></div><div className="field full"><label>Street address</label><input className="control" name="address" required/></div><div className="field"><label>City</label><input className="control" name="city" required/></div><div className="field"><label>Postal code</label><input className="control" name="postalCode" required/></div></div></section><section className="panel"><h3>Payment method</h3><div className="payment-choice">{settings.codEnabled&&<label className={`payment-card ${method==='COD'?'active':''}`}><input type="radio" checked={method==='COD'} onChange={()=>setMethod('COD')}/><Truck/><div><b>Cash on Delivery (COD)</b><p className="muted">Place the order now and pay when it arrives.</p></div></label>}{settings.qrEnabled&&<label className={`payment-card ${method==='QR'?'active':''}`}><input type="radio" checked={method==='QR'} onChange={()=>setMethod('QR')}/><QrCode/><div><b>QR Payment</b><p className="muted">Scan the store QR, pay, then upload proof for admin verification.</p></div></label>}</div>{method==='QR'&&<div className="qr-box"><img src={settings.qrImage||'/store-qr.svg'} alt="Store payment QR code"/><b>Scan and complete your payment</b><p className="muted">Then upload a clear screenshot and enter the exact transaction/reference ID.</p><div className="field" style={{textAlign:'left',marginTop:14}}><label>Payment screenshot</label><input className="control" type="file" accept="image/*" required onChange={e=>handleProof(e.target.files?.[0])}/>{proofPreview&&<img className="upload-preview" src={proofPreview} alt="Payment proof preview"/>}</div><div className="field" style={{textAlign:'left',marginTop:12}}><label>Transaction / Reference ID</label><input className="control" value={txid} onChange={e=>setTxid(e.target.value)} required placeholder="e.g. TXN123456789"/></div></div>}</section></div><aside className="panel" style={{height:'max-content'}}><h3>Your order</h3>{cartProducts.map(({product,quantity})=><div className="summary-row" key={product.id}><span>{product.name} × {quantity}</span><b>{money(product.price*quantity)}</b></div>)}<hr style={{border:0,borderTop:'1px solid var(--line)'}}/><div className="field"><label>Promo code</label><div style={{display:'flex',gap:8}}><input className="control" value={promo} onChange={e=>setPromo(e.target.value)} placeholder="EASY10"/></div>{promo&&<small style={{color:validPromo?'var(--success)':'var(--danger)'}}>{validPromo?`Applied: ${validPromo.code}`:'Code not valid'}</small>}</div><div className="summary-row"><span>Subtotal</span><b>{money(subtotal)}</b></div><div className="summary-row"><span>Shipping</span><b>{shipping?money(shipping):'Free'}</b></div>{discount>0&&<div className="summary-row"><span>Discount</span><b>−{money(discount)}</b></div>}<div className="summary-row total"><span>Total</span><span>{money(total)}</span></div><button disabled={busy||(!settings.codEnabled&&!settings.qrEnabled)} className="btn sage" style={{width:'100%',marginTop:14}}>{busy?'Placing order…':method==='QR'?'Submit payment for verification':'Place COD order'}</button><p className="muted" style={{fontSize:'.72rem',marginTop:12}}>By ordering, you agree to the store’s shipping and return policy.</p></aside></form></div>
}
