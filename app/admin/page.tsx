'use client';
import AdminShell from '@/components/admin-shell';
import { useStore } from '@/components/store-provider';
import { money } from '@/lib/format';
import { Boxes, Package, ShoppingCart, Users } from 'lucide-react';
export default function AdminDashboard(){const {orders,products}=useStore();const sales=orders.filter(o=>o.status!=='Payment Rejected').reduce((s,o)=>s+o.total,0);const customers=new Set(orders.map(o=>o.customer.email)).size;const bars=[35,52,42,70,58,88,64];return <AdminShell><div className="admin-top"><div><span className="eyebrow">Store control room</span><h1>Dashboard</h1><p className="muted">A quick read on the thrift store.</p></div></div><div className="stat-grid"><div className="stat"><div className="icon"><ShoppingCart size={18}/></div><b>{money(sales)}</b><span>Total sales</span></div><div className="stat"><div className="icon"><Package size={18}/></div><b>{orders.length}</b><span>Orders</span></div><div className="stat"><div className="icon"><Boxes size={18}/></div><b>{products.length}</b><span>Products</span></div><div className="stat"><div className="icon"><Users size={18}/></div><b>{customers}</b><span>Customers</span></div></div><div className="admin-grid"><div className="admin-card"><h3>Recent orders</h3>{orders.length?<table className="data-table"><thead><tr><th>Order</th><th>Customer</th><th>Payment</th><th>Status</th><th>Total</th></tr></thead><tbody>{orders.slice(0,6).map(o=><tr key={o.id}><td><b>{o.id}</b></td><td>{o.customer.name}<br/><span className="muted">{o.customer.email}</span></td><td>{o.paymentMethod}</td><td><span className="status">{o.status}</span></td><td>{money(o.total)}</td></tr>)}</tbody></table>:<div className="empty-state" style={{padding:30}}>No orders yet. Place a demo checkout from the storefront.</div>}</div><div className="admin-card"><h3>Sales overview</h3><p className="muted">Demo weekly visual. Connect your database aggregate for real reporting.</p><div className="chart-bars">{bars.map((h,i)=><div className="chart-bar" style={{height:`${h}%`}} key={i}><span>{['M','T','W','T','F','S','S'][i]}</span></div>)}</div></div></div></AdminShell>}
