'use client';
import { FormEvent } from 'react';import AdminShell from '@/components/admin-shell';import { useStore } from '@/components/store-provider';import { PromoCode } from '@/lib/types';
export default function Discounts(){const {promos,savePromos}=useStore();function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const item:PromoCode={id:`promo-${Date.now()}`,code:String(f.get('code')).toUpperCase(),type:String(f.get('type')) as 'percentage'|'fixed',value:Number(f.get('value')),expiresAt:String(f.get('expiresAt')),active:true};savePromos([item,...promos]);e.currentTarget.reset()}return <AdminShell><div className="admin-top"><div><span className="eyebrow">Promotions</span><h1>Discounts</h1><p className="muted">Create fixed or percentage promo codes with expiration dates.</p></div></div><div className="admin-grid"><div className="admin-card"><h3>Active + saved codes</h3><table className="data-table"><thead><tr><th>Code</th><th>Type</th><th>Value</th><th>Expires</th><th>Status</th><th></th></tr></thead><tbody>{promos.map(p=><tr key={p.id}><td><b>{p.code}</b></td><td>{p.type}</td><td>{p.type==='percentage'?`${p.value}%`:`$${p.value}`}</td><td>{p.expiresAt}</td><td><button className="mini-btn" onClick={()=>savePromos(promos.map(x=>x.id===p.id?{...x,active:!x.active}:x))}>{p.active?'Active':'Disabled'}</button></td><td><button className="mini-btn danger" onClick={()=>savePromos(promos.filter(x=>x.id!==p.id))}>Delete</button></td></tr>)}</tbody></table></div><div className="admin-card"><h3>Create promo</h3><form className="stack" onSubmit={add}><div className="field"><label>Code</label><input className="control" name="code" required placeholder="THRIFT15"/></div><div className="field"><label>Discount type</label><select className="control" name="type"><option value="percentage">Percentage</option><option value="fixed">Fixed amount</option></select></div><div className="field"><label>Value</label><input className="control" name="value" type="number" min="1" step="0.01" required/></div><div className="field"><label>Expiration date</label><input className="control" name="expiresAt" type="date" required/></div><button className="btn sage">Create code</button></form></div></div></AdminShell>}
