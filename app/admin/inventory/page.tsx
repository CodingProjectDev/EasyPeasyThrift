'use client';
import AdminShell from '@/components/admin-shell';import { useStore } from '@/components/store-provider';
import { ProductImage } from '@/components/product-image';
export default function Inventory(){const {products,updateProduct}=useStore();return <AdminShell><div className="admin-top"><div><span className="eyebrow">Stock control</span><h1>Inventory</h1><p className="muted">One-of-one pieces should stay at stock 1 until sold, then drop to 0.</p></div></div><div className="admin-card"><table className="data-table"><thead><tr><th>Product</th><th>Type</th><th>Current stock</th><th>Alert</th><th>Set stock</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><div className="table-product"><ProductImage className="thumb" src={p.images[0]} alt={p.name}/><b>{p.name}</b></div></td><td>{p.oneOfOne?'One-of-One':'Standard'}</td><td><b>{p.inventory}</b></td><td>{p.inventory===0?<span className="status bad">Sold Out</span>:p.inventory<=1?<span className="status warn">Low stock</span>:<span className="status good">In stock</span>}</td><td><input className="control" style={{width:90}} type="number" min="0" value={p.inventory} onChange={e=>updateProduct({...p,inventory:Number(e.target.value)})}/></td></tr>)}</tbody></table></div></AdminShell>}
