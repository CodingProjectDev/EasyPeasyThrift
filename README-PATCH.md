# Patch note

The older patch instructions are superseded by the current project.

For an existing Supabase database, run:

`supabase/FINAL-FIXES.sql`

Then configure the environment variables documented in `README.md` and redeploy.
